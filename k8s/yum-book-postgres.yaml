apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: yum-book
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: django
            - name: POSTGRES_PASSWORD
              value: django
            - name: POSTGRES_DB
              value: django
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: yum-book
spec:
  ports:
    - port: 5432
  selector:
    app: postgres